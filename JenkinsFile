pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                git 'https://github.com/lucasbernardessantos/tennis.git'
                sh '.\mvnw clean compile'
            }
        }
    }
}